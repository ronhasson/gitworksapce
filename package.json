{
  "name": "git-workspace-mcp-extension",
  "version": "1.0.0",
  "description": "Intelligent Git workspace management with AI-guided file operations",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "test": "echo \"No tests yet\" && exit 0",
    "dev": "node --inspect server/index.js",
    "build": "echo \"No build process required for pure Node.js extension\"",
    "validate": "node --input-type=module -e \"import {readFileSync} from 'fs'; JSON.parse(readFileSync('manifest.json', 'utf8')); console.log('✅ Manifest is valid JSON');\"",
    "package": "zip -r git-workspace-v1.0.0.dxt manifest.json server/ package.json README.md LICENSE icon.svg -x '.git/*' '*.dxt'",
    "clean": "rm -f *.dxt",
    "inspect": "npx @modelcontextprotocol/inspector node server/index.js",
    "help": "echo '📚 Available commands:' && echo '  npm run start     - Start the MCP server' && echo '  npm run dev       - Start with debugging' && echo '  npm run validate  - Validate manifest.json' && echo '  npm run package   - Create .dxt file' && echo '  npm run inspect   - Debug with MCP inspector'"
  },
  "keywords": [
    "mcp",
    "git",
    "workspace", 
    "filesystem",
    "ai",
    "development",
    "productivity"
  ],
  "author": {
    "name": "Ron Hasson"
  },
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.4.0",
    "diff": "^5.1.0",
    "minimatch": "^9.0.3"
  },
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "https://github.com/ronhasson/gitworksapce"
  },
  "bugs": {
    "url": "https://github.com/ronhasson/gitworksapce/issues"
  },
  "homepage": "https://github.com/ronhasson/gitworksapce"
}